<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/index.css"/>
    <title>Edit Product</title>
  </head>
  <body>
    <% @product.each do |item| %>
      <form action="/products/<%= item['id'] %>" method="post">
        <input type="hidden" name="_method" value="put">
        <div>
          <label for="name">Product Name</label>
          <input type="text" name="name" id="name" placeholder="Product Name" required value="<%= item['name'] %>">
        </div>
        <div>
          <label for="price">Product Price</label>
          <input type="text" name="price" id="price" placeholder="Product Price" required value="<%= item['price'] %>">
        </div>
        <button type="submit">Edit Product</button>
      </form>
    <% end %>
  </body>
</html>
